{% extends "::layout.html.twig" %}

{% block title %}Hey{% endblock %}

{% block content %}

    <section class="row">
        <h1>Category list </h1>
        {% for tag in tags %}

            <h2 class="text-center"><a href="{{ url('tag_show', {'id': tag.id}) }}"> {{ tag.name }}</a></h2>

        {% endfor %}
    </section>

{% endblock %}